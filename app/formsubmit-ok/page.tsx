/** Landing page after hidden FormSubmit iframe POST (detect success via iframe URL) */
export default function FormSubmitOkPage() {
  return (
    <main className="flex min-h-[40vh] items-center justify-center p-8 text-center">
      <p className="text-sm text-navy-600">Thank you — you can close this tab.</p>
    </main>
  );
}
